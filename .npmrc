# Force npm to install devDependencies even when NODE_ENV=production.
# Required because Next.js needs tailwindcss / postcss / typescript at
# build time, and Railway sets NODE_ENV=production for both build and
# runtime phases.
production=false
